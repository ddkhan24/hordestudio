const fs=require('fs'),vm=require('vm'),assert=require('assert/strict');const src=fs.readFileSync('app.js','utf8');const start=src.indexOf('function applyBuiltCompanionLife('),end=src.indexOf('\nfunction setupCompanionsLogic',start);const ctx={safeJsonClone:x=>JSON.parse(JSON.stringify(x)),normalizeCompanionLifeProfile:x=>({places:x.places||[]}),normalizeCompanionLifeRuntime:x=>x,companionLocalMinuteInfo:()=>({dateKey:'test'}),companionSituationAt:()=>({})};vm.createContext(ctx);vm.runInContext(fs.readFileSync('vh-life-schema.js','utf8'),ctx);vm.runInContext(src.slice(start,end),ctx);const built={institutions:[{id:'class-attendance',label:'Class attendance',scheduleId:'class',minimumAttendance:.5,fee:0,missedStress:2}],places:[],rooms:[{id:'room'}],peopleLives:[{personId:'friend'}],routes:[{from:'a',to:'b'}],referencePlan:[{id:'ref'}],finance:{enabled:true},styleProfiles:[{id:'style'}]};const c={id:'c'};ctx.applyBuiltCompanionLife(c,built,1);for(const key of ['institutions','rooms','peopleLives','routes','referencePlan','finance'])assert.equal(JSON.stringify(c.lifeSetupPolicies[key]),JSON.stringify(built[key]));built.rooms[0].id='mutated';assert.equal(c.lifeSetupPolicies.rooms[0].id,'room');assert.equal(c.lifeStyleProfiles[0].id,'style');console.log('PASS: builder preserves executable policies and clones reviewed draft data.');
